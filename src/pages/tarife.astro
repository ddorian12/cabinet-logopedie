---
import Layout from "../layouts/Layout.astro";
---
<Layout title="Tarife – Logopedie" description="Tarife transparente pentru evaluare logopedică, ședințe de terapie și pachete.">
  <section class="relative max-w-5xl mx-auto px-6 py-20">
    <!-- Hero Header -->
    <div class="text-center mb-12">
      <div class="inline-block px-5 py-3 rounded-full bg-white/80 backdrop-blur-md text-indigo-600 font-semibold text-sm mb-6 shadow-neumorphic" aria-hidden="true">
        💸 Tarifele noastre
      </div>
      <h1
        class="text-4xl md:text-5xl font-extrabold mb-6 leading-[1.2] tracking-tight text-balance"
      >
        Tarife transparente pentru servicii de
        <span class="inline-block bg-linear-to-r from-indigo-600 to-purple-500 bg-clip-text text-transparent pb-1">
          logopedie
        </span>
      </h1>
      <p class="text-xl text-gray-600 max-w-3xl mx-auto leading-relaxed">
        Investește în progresul tău cu servicii personalizate la prețuri clare
      </p>
    </div>

    <!-- Pricing Cards -->
    <div class="grid gap-6 md:grid-cols-3">
      <!-- Evaluare -->
      <article class="group bg-white/80 backdrop-blur-md rounded-3xl p-8 shadow-neumorphic hover:shadow-xl transition-all duration-300">
        <div class="w-16 h-16 rounded-2xl bg-linear-to-br from-indigo-500 to-indigo-600 flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform duration-300" aria-hidden="true">
          🔍
        </div>
        <h2 class="text-xl font-bold mb-3 text-gray-900">Evaluare logopedică</h2>
        <p class="text-gray-600 text-base mb-4">Anamneză, testare/probe + raport scurt (60–75&nbsp;min)</p>
        <div class="text-2xl font-bold text-indigo-600" aria-label="Preț evaluare">xxx lei</div>
      </article>

      <!-- Ședință -->
      <article class="group bg-white/80 backdrop-blur-md rounded-3xl p-8 shadow-neumorphic hover:shadow-xl transition-all duration-300">
        <div class="w-16 h-16 rounded-2xl bg-linear-to-br from-purple-500 to-purple-600 flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform duration-300" aria-hidden="true">
          💬
        </div>
        <h2 class="text-xl font-bold mb-3 text-gray-900">Ședință terapie</h2>
        <p class="text-gray-600 text-base mb-4">Individual, obiective personalizate (45–50&nbsp;min)</p>
        <div class="text-2xl font-bold text-purple-600" aria-label="Preț ședință">xxx lei</div>
      </article>

      <!-- Pachet -->
      <article class="group bg-white/80 backdrop-blur-md rounded-3xl p-8 shadow-neumorphic hover:shadow-xl transition-all duration-300">
        <div class="w-16 h-16 rounded-2xl bg-linear-to-br from-pink-500 to-pink-600 flex items-center justify-center text-3xl mb-6 group-hover:scale-110 transition-transform duration-300" aria-hidden="true">
          📚
        </div>
        <h2 class="text-xl font-bold mb-3 text-gray-900">Pachet 4 ședințe</h2>
        <p class="text-gray-600 text-base mb-4">Valabil 30 de zile</p>
        <div class="text-2xl font-bold text-pink-600" aria-label="Preț pachet">xxx lei</div>
      </article>
    </div>

    <!-- Additional Info -->
    <p class="text-gray-500 text-sm mt-8 text-center">
      * Plata se face la ședință; reprogramarea se anunță cu min. 24h înainte. Prețurile includ TVA (dacă este cazul).
    </p>

    <!-- CTA Button -->
    <div class="text-center mt-10">
      <a href="/contact" class="inline-flex items-center gap-3 bg-linear-to-r from-indigo-600 to-purple-600 text-white px-10 py-5 rounded-xl font-bold text-xl shadow-lg hover:shadow-xl hover:scale-105 transition-all duration-300">
        <span>Programează acum</span>
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24" aria-hidden="true">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
        </svg>
      </a>
    </div>

    <!-- Decorative Orbs -->
    <div aria-hidden="true" class="absolute w-80 h-80 bg-linear-to-br from-indigo-400/20 to-purple-400/20 rounded-full top-0 left-0 blur-3xl animate-pulse"></div>
    <div aria-hidden="true" class="absolute w-64 h-64 bg-linear-to-br from-pink-400/20 to-orange-400/20 rounded-full bottom-10 right-0 blur-3xl animate-pulse delay-1000"></div>
  </section>

  <style>
    /* Neumorphic shadow for a soft, embossed effect */
    .shadow-neumorphic {
      box-shadow: 10px 10px 20px rgba(0, 0, 0, 0.1), -10px -10px 20px rgba(255, 255, 255, 0.7);
    }

    /* Floating animation for orbs */
    .animate-float { animation: float 6s ease-in-out infinite; }

    @keyframes float {
      0%, 100% { transform: translateY(0); }
      50%      { transform: translateY(-20px); }
    }

    /* Respectă preferința de a reduce animațiile */
    @media (prefers-reduced-motion: reduce) {
      .animate-float,
      .animate-pulse { animation: none !important; }
      .hover\:scale-105:hover { transform: none !important; }
    }
  </style>
</Layout>
